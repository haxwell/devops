This is the root of the Haxwell Infrastructure file structure. Put it on a server somewhere, and let Haxwell Ionic Apps get the config files they need to quickly create stable backend instances.

Run the list of commands in the DEVELOPER file (read some info in the DEVOPS file), and then run the ./new-server-install.sh script.
